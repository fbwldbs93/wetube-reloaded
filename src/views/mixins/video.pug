mixin video(video)
    div
        h4 
            a(href=`/videos/${video.id}`)=video.title
            //-#{변수}는 HTML 속성에는 사용할 수 없음
            //-ex: href, id, class 등등
        p=video.description
        ul
            each hashtag in video.hashtags
                li=hashtag
        small=video.createdAt
        hr
